I dataset caricati vengono ridotti con l'RSVD, scelgo un proporzione in modo tale da andare a ridurre le caratteristiche (colonne) dei file di dati

Creare la matrice di affinità: matrice quadrata che indica tramite una misura la somiglianza tra due punti, i e j.

La calcolo attraverso snf_test.py (codice snf github)

unire le matrici di affinità per ogni tipo di tessuto/tumore.

graphsage: env su python 3.5, pip sui requirements e example test su supervisionato e non supervisionato

creazione file json per input a graphsage.

supervisionato, con quegli input
stessa cosa aggiungendo le feature.

walk.txt condizione in più

tensorboard --logdir=unsup-example_data
Vai a http://localhost:6006 nel tuo browser per vedere le visualizzazioni.

Per le classi molto probabilmente devo impostare la sigmoid_loss a true (capire questa cosa appena rivedo il codice graphsage)




to do: 
sul git manca **MODIFICHE.md**



